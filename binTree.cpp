#include "binTree.h"

EmployeeTree* EmployeeTree::addNode(EmployeeTree* tree, Employee data)
{
	if (tree == nullptr) {
		EmployeeTree* temp = new EmployeeTree();
		temp->emp = data;

	}
	return nullptr;
}
